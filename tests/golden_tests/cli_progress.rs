//! Golden tests of `dstore-cli` `progress` (owner cli-progress): `cli/text.json` `status_line` and
//! `fraction`, and the other progress sections of that file (`rate_meter`, `node_state`, `tea_handler`,
//! `format_event`, `blend1d`, `progress_bar`, `ui_model`) where they reach public APIs.
